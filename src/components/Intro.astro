---
---
<div>
  <h1>
    <span id="typewriter-heading" class="typewriter-heading hidden"></span><span id="cursor" class="cursor hidden">|</span>
  </h1>
  <img src="/images/ich.jpg" alt="Profilbild" class="profile-pic">
  <p id="intro-paragraph">
    Ich bin Abdallah Ben-Salah,
    <br>
    leidenschaftlicher IT-Developer und Unternehmer aus dem schönen Hamburg.
    <br>
    Ich beschäftige mich mit <b>Webentwicklung</b>, <b>Digitalisierung</b>, <b>IT-Sicherheit</b> und vielem mehr.
    <br>
    Mit kreativen Ideen und einem klaren Blick für Innovationen entwickle ich digitale Lösungen für Privatpersonen und Unternehmen.
    <br>
    Weiter unten findest du Projekte, an denen ich gearbeitet habe.
    <br><br>
    Hast du Fragen oder möchtest dich austauschen? Dann nutze gerne das <strong>Kontaktformular</strong>. 
    <br>
    Schau auch in meinem <b>Blog</b> vorbei!
    <br>
    Bis bald 🚀
  </p>
  <button class="btn-transition" id="toggleBtn">
    Mehr über mich <span class="arrow">→</span>
  </button>
</div>

<style>
  .typewriter-heading {
    font-family: inherit;
    color: inherit;
    font-size: inherit;
    display: inline;
  }

  .cursor {
    font-family: inherit;
    font-size: inherit;
    color: inherit;
    display: inline-block;
    margin-left: 2px;
    animation: blink 1s steps(2, start) infinite;
  }

  @keyframes blink {
    50% {
      opacity: 0;
    }
  }

  .hidden {
    display: none;
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const text = "Moin und Willkommen :)";
    const typewriterHeading = document.getElementById('typewriter-heading');
    const cursor = document.getElementById('cursor');
    const toggleBtn = document.getElementById('toggleBtn');
    const introParagraph = document.getElementById('intro-paragraph');
    const profilePic = document.querySelector('.profile-pic');
    let showingMore = false;
    let index = 0;

    // Text-Effekt für Typewriter
    function typeEffect() {
      if (index < text.length) {
        typewriterHeading.textContent += text[index];
        index++;
        setTimeout(typeEffect, 100);
      } else {
        cursor.classList.remove('hidden');
      }
    }

    // Startet Typewriter
    function startTypewriter() {
      typewriterHeading.classList.remove('hidden');
      cursor.classList.remove('hidden');
      typewriterHeading.textContent = ""; // Reset
      index = 0;
      typeEffect();
    }

    // Stoppt Typewriter
    function stopTypewriter() {
      typewriterHeading.classList.add('hidden');
      cursor.classList.add('hidden');
      typewriterHeading.textContent = "";
    }

    // Content-Toggle zwischen Intro und More
    const texts = {
      intro: {
        paragraph: `
          Ich bin Abdallah Ben-Salah,
          <br>
          leidenschaftlicher IT-Developer und Unternehmer aus dem schönen Hamburg.
          <br>
          Ich beschäftige mich mit <b>Webentwicklung</b>, <b>Digitalisierung</b>, <b>IT-Sicherheit</b> und vielem mehr.
          <br>
          Mit kreativen Ideen und einem klaren Blick für Innovationen entwickle ich digitale Lösungen für Privatpersonen und Unternehmen.
          <br>
          Weiter unten findest du Projekte, an denen ich gearbeitet habe.
          <br><br>
          Hast du Fragen oder möchtest dich austauschen? Dann nutze gerne das <strong>Kontaktformular</strong>. 
          <br>
          Schau auch in meinem <b>Blog</b> vorbei!
          <br>
          Bis bald 🚀
        `,
        buttonText: "Mehr über mich <span class='arrow'>→</span>"
      },
      more: {
        paragraph: `
          <div class="block-container">
            <div class="block">
              <h2>Fähigkeiten und Interessen</h2>
              <ul>
                <li><b>Webentwicklung</b> (HTML, CSS, JS, Astro...)</li>
                <li><b>Backend-Entwicklung</b> (Python, Java, Node.js...)</li>
                <li><b>DevOps</b> (Netlify...)</li>
                <li><b>IT-Sicherheit</b> (Forensic, Network, Web Security...)</li>
                <li><b>Digitalisierung</b> (Automatisierung, Prozessoptimierung, KI...)</li>
              </ul>
              <h2>Aktuell</h2>
              Zurzeit beschäftige ich mich mit dem Web-Framework <b>Astro</b> und der Cloud Web Platform <b>Netlify</b><br>
            </div>
          </div>
        `,
        buttonText: "Zurück <span class='arrow'>←</span>"
      }
    };

    function toggleContent() {
      const content = showingMore ? texts.intro : texts.more;
      introParagraph.innerHTML = content.paragraph;
      toggleBtn.innerHTML = content.buttonText;

      if (showingMore) {
        startTypewriter();
        profilePic.classList.remove('hidden');
      } else {
        stopTypewriter();
        profilePic.classList.add('hidden');
      }

      showingMore = !showingMore;
      toggleBtn.blur();
    }

    // Startet den Typewriter-Effekt beim Laden der Seite
    startTypewriter();

    // Event-Listener für den Button
    toggleBtn.addEventListener('click', toggleContent);
  });
</script>
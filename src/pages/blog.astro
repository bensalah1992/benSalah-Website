---
import BaseLayout from '../layouts/BaseLayout.astro';
import BlogPost from '../components/BlogPost.astro';
const allPosts = await Astro.glob('./posts/*.md');

const pageTitle = "Tech Blog";
---

<BaseLayout pageTitle={pageTitle}>
  <ul class="blog-grid">
    {allPosts.map((post) => (
      <BlogPost url={post.url} title={post.frontmatter.title} image={post.frontmatter.image.url} />
    ))}
  </ul>
</BaseLayout>

<style>
  .blog-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr); /* Zwei Spalten */
    gap: 20px; /* Abstand zwischen den Posts */
    list-style: none;
    padding: 0;
  }

  @media (max-width: 768px) {
    .blog-grid {
      grid-template-columns: 1fr; /* Eine Spalte bei kleineren Bildschirmen */
    }
  }
</style>